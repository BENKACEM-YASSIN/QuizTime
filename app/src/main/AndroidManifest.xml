<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quiztime.quiztime">

    <application
        android:allowBackup="true"
        android:icon="@drawable/qtl"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Welcom"
            android:theme="@style/full">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme" />
        <activity android:name=".signup" />
        <activity android:name=".GameActivity" />
        <activity android:name=".QuestActivity" />
        <activity android:name=".CountriesQuestions" />
        <activity android:name=".EachCategorieHighScore" />
        <activity android:name=".CelebrityQuestions" />
        <activity android:name=".BrandsQuestions"></activity>
    </application>

</manifest>